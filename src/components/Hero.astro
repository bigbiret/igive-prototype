---
const trustStats = {
  merchants: '16.000+',
  activeCards: '2 millioner',
  totalBalance: '352 millioner'
};
---

<section id="hero" class="relative pt-20 pb-16 lg:pt-28 lg:pb-20" style="background: #FEF4E9 !important; background-image: none !important;">
  <div class="container mx-auto px-6">
    <div class="max-w-7xl mx-auto">
      
      <!-- Main Hero Content -->
      <div class="text-center max-w-4xl mx-auto mb-12">
        <!-- Trust Indicator -->
        <div class="inline-flex items-center gap-2 bg-white px-4 py-2 rounded-full shadow-sm mb-6">
          <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
          </svg>
          <span class="text-sm font-medium text-gray-700">Norges ledende gavekortplattform</span>
        </div>
        
        <!-- Main Headline -->
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-heading font-bold text-igive-heading mb-6">
          Teknologi som gjør gavekort
          <span id="rotating-word-container" class="inline-block ml-2 transition-all duration-500" style="width: 180px;">
            <span id="rotating-word" class="text-[#8B7355] inline-block transition-all duration-700 ease-in-out">enklere</span>
          </span>
        </h1>
        
        <!-- Sub-headline -->
        <h2 class="text-xl md:text-2xl text-igive-text mb-10 max-w-3xl mx-auto leading-relaxed">
          Lukkede gavekort med full kontroll på brukersteder, beløp og varighet - iGive tar hånd om drift, samsvar og support
        </h2>
        
        <!-- Trust Elements -->
        <div class="flex flex-wrap justify-center gap-4 text-xs text-gray-500 mb-8">
          <div class="flex items-center gap-1">
            <span class="text-sm">🇳🇴</span>
            <span>100% norsk-eid</span>
          </div>
          <div class="flex items-center gap-1">
            <svg class="w-3 h-3 text-purple-500" fill="currentColor" viewBox="0 0 20 20">
              <path d="M4 4a2 2 0 00-2 2v1h16V6a2 2 0 00-2-2H4zM18 9H2v5a2 2 0 002 2h12a2 2 0 002-2V9zM4 13a1 1 0 011-1h1a1 1 0 110 2H5a1 1 0 01-1-1zm5-1a1 1 0 100 2h1a1 1 0 100-2H9z"></path>
            </svg>
            <span>Visa Principal Member</span>
          </div>
          <div class="flex items-center gap-1">
            <svg class="w-3 h-3 text-blue-500" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span>GDPR & KYC-godkjent</span>
          </div>
        </div>
        
        <!-- Primary CTA Heading -->
        <div class="text-center mb-8">
          <h3 class="text-2xl md:text-3xl font-semibold text-igive-heading">
            Utforsk løsningene
          </h3>
        </div>
      </div>
      
      <!-- Service Cards Grid -->
      <div class="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto mb-12">
        <a href="#visa" class="group bg-white rounded-xl p-4 shadow-sm hover:shadow-xl transform hover:-translate-y-1 transition-all duration-200 border border-gray-100 relative">
          <!-- Nyhet badge -->
          <div class="absolute -top-2 -right-2 bg-igive-primary text-white text-xs px-2 py-1 rounded-full font-medium">
            Nyhet
          </div>
          <div class="flex items-center gap-3 mb-2">
            <div class="w-10 h-10 bg-igive-primary/10 rounded-lg flex items-center justify-center group-hover:bg-igive-primary/20 transition-colors">
              <svg class="w-5 h-5 text-igive-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a1 1 0 001-1V4a1 1 0 00-1-1H8a1 1 0 00-1 1v16a1 1 0 001 1z"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-igive-heading group-hover:text-igive-primary transition-colors">
              Visa digitalt gavekort
            </h3>
          </div>
          <p class="text-sm text-gray-600 mb-3">
            Digitalt gavekort. Apple & Google Wallet støtte.
          </p>
          <span class="text-igive-primary font-medium text-sm flex items-center gap-1">
            Les mer
            <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </span>
        </a>
        
        <a href="#pos" class="group bg-white rounded-xl p-4 shadow-sm hover:shadow-xl transform hover:-translate-y-1 transition-all duration-200 border border-gray-100">
          <div class="flex items-center gap-3 mb-2">
            <div class="w-10 h-10 bg-igive-primary/10 rounded-lg flex items-center justify-center group-hover:bg-igive-primary/20 transition-colors">
              <svg class="w-5 h-5 text-igive-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-igive-heading group-hover:text-igive-primary transition-colors">
              POS-integrert
            </h3>
          </div>
          <p class="text-sm text-gray-600 mb-3">
            Digitalt gavekort – betal med QR fra kundens mobil.
          </p>
          <span class="text-igive-primary font-medium text-sm flex items-center gap-1">
            Les mer
            <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </span>
        </a>
        
        <a href="#fysisk" class="group bg-white rounded-xl p-4 shadow-sm hover:shadow-xl transform hover:-translate-y-1 transition-all duration-200 border border-gray-100">
          <div class="flex items-center gap-3 mb-2">
            <div class="w-10 h-10 bg-igive-primary/10 rounded-lg flex items-center justify-center group-hover:bg-igive-primary/20 transition-colors">
              <svg class="w-5 h-5 text-igive-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-igive-heading group-hover:text-igive-primary transition-colors">
              Fysiske gavekort
            </h3>
          </div>
          <p class="text-sm text-gray-600 mb-3">
            Plastkort med egen design og merkevare.
          </p>
          <span class="text-igive-primary font-medium text-sm flex items-center gap-1">
            Les mer
            <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </span>
        </a>
      </div>
      
      
    </div>
  </div>
</section>

<script>
  // Rotating words animation with smooth container width transition
  const words = ['enklere', 'smartere', 'raskere', 'tryggere', 'moderne', 'fleksible', 'effektive', 'skalerbare', 'intuitive', 'pålitelige', 'lønnsomt', 'sømløse'];
  let currentIndex = 0;
  const rotatingElement = document.getElementById('rotating-word');
  const container = document.getElementById('rotating-word-container');
  
  // Calculate and set the max width needed to prevent layout shift
  if (rotatingElement && container) {
    // Create temporary element to measure word widths
    const measurer = document.createElement('span');
    measurer.style.position = 'absolute';
    measurer.style.visibility = 'hidden';
    measurer.style.whiteSpace = 'nowrap';
    measurer.className = rotatingElement.className;
    document.body.appendChild(measurer);
    
    // Find the widest word
    let maxWidth = 0;
    words.forEach(word => {
      measurer.textContent = word;
      const width = measurer.offsetWidth;
      if (width > maxWidth) maxWidth = width;
    });
    
    // Set container to max width to prevent jumping
    container.style.width = maxWidth + 10 + 'px'; // Add small buffer
    document.body.removeChild(measurer);
    
    // Start animation
    setInterval(() => {
      // Current word exits downward
      rotatingElement.style.opacity = '0';
      rotatingElement.style.transform = 'translateY(40px)';
      
      setTimeout(() => {
        // Change word while invisible
        currentIndex = (currentIndex + 1) % words.length;
        rotatingElement.textContent = words[currentIndex];
        
        // Position new word above (invisible)
        rotatingElement.style.transition = 'none';
        rotatingElement.style.transform = 'translateY(-40px)';
        
        // Re-enable transition and bring word down from top
        setTimeout(() => {
          rotatingElement.style.transition = 'all 0.7s ease-in-out';
          rotatingElement.style.opacity = '1';
          rotatingElement.style.transform = 'translateY(0)';
        }, 50);
      }, 700); // Wait for exit animation to complete
    }, 3500); // Slightly longer interval for better readability
  }

  // Smooth scroll for anchor links
  document.addEventListener('DOMContentLoaded', () => {
    // Handle all anchor links with smooth scrolling
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const targetId = (this as HTMLAnchorElement).getAttribute('href');
        if (targetId && targetId !== '#') {
          const targetElement = document.querySelector(targetId);
          if (targetElement) {
            targetElement.scrollIntoView({
              behavior: 'smooth',
              block: 'start'
            });
          }
        }
      });
    });
  });
</script>

<style>
  .badge-igive {
    @apply inline-block px-4 py-2 bg-igive-button text-igive-text rounded-full text-sm;
  }
</style>